This project includes two Python programs that use lists to store and process data. The first program identifies the day with the highest sales from a predefined list of weekly sales values. The second program allows users to input numbers into a list and calculates the range by finding the difference between the highest and lowest values. Both programs use basic control structures and display results using formatted output.
